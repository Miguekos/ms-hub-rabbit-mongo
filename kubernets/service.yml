apiVersion: v1
kind: Service
metadata:
  namespace: hub-dev
  name: ms-hub-service
  labels:
    app: ms-hub-app
spec:
  type: NodePort
  ports:
    - port: 3349
      targetPort: 3349
      nodePort: 30349
      name: "ms-hub-service"
  selector:
    app: ms-hub-app
