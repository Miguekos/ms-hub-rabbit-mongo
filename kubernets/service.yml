apiVersion: v1
kind: Service
metadata:
  namespace: hub-dev
  name: ms-hub-service
  labels:
    app: ms-hub-service
spec:
  type: NodePort
  selector:
    app: ms-hub
  ports:
    - port: 3349
      targetPort: 3349
      nodePort: 30349
      name: http
